<header>
  <nav class="nav">
    <div class="nav-left">
      <%= link_to 'Головна', root_path, class: 'nav-link logo'%>
    </div>
      <% if user_signed_in? %>
        <div class="nav-right">
          <%= link_to 'Новий пост', new_post_path %>
          <%= link_to 'Мій профіль', edit_user_registration_path %>
          <%= link_to 'Вийти', destroy_user_session_path, 
                data: { turbo_method: :delete } %>
      <% else %>
          <%= link_to 'Увійти', new_user_session_path %>
          <%= link_to 'Зареєструватися', new_user_registration_path %>
      <% end %>
        </div>
  </nav>
</header>
